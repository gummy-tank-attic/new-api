# DISABLED 2026-08-17: www is VPS nginx. Pages project metartr-web is deleted.
# Ship frontend from the parent repo: .\scripts\deploy-web.ps1
# Do not re-enable this workflow.
name: Deploy Frontend (Pages disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Pages is decommissioned
        run: |
          echo "::error::Cloudflare Pages is gone. Use parent .\\scripts\\deploy-web.ps1"
          exit 1
